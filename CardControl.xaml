<UserControl x:Class="PokerGame.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="240" Width="160">
    <UserControl.Resources>
        <Storyboard x:Key="FlipClose">
            <DoubleAnimation Storyboard.TargetName="MainGrid" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                             To="0" Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="FlipOpen">
            <DoubleAnimation Storyboard.TargetName="MainGrid" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                             To="1" Duration="0:0:0.15"/>
        </Storyboard>
        <Storyboard x:Key="WinAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="WinBorder"
                             Storyboard.TargetProperty="Opacity"
                             From="0.4" To="1.0" Duration="0:0:0.5"
                             AutoReverse="True"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="1"/>
        </Grid.RenderTransform>

        <!-- Card Back -->
        <Border x:Name="CardBack" Background="#003366" CornerRadius="12" BorderBrush="White" BorderThickness="3" Visibility="Visible">
            <Grid>
                <Rectangle Fill="Transparent"/> <!-- Hit test -->
                <TextBlock Text="QVP" Foreground="#6699CC" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="40" Opacity="0.5">
                    <TextBlock.RenderTransform>
                        <RotateTransform Angle="-45"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
                <Border BorderBrush="#6699CC" BorderThickness="2" Margin="8" CornerRadius="6" Opacity="0.5"/>
            </Grid>
        </Border>

        <!-- Card Front -->
        <Border x:Name="CardFront" Background="White" CornerRadius="12" BorderBrush="Black" BorderThickness="1" Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <!-- Top Left Rank/Suit -->
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8">
                    <TextBlock x:Name="TopRank" Text="A" FontSize="32" FontWeight="Bold"/>
                    <TextBlock x:Name="TopSuit" Text="♠" FontSize="32"/>
                </StackPanel>

                <!-- Center Suit -->
                <TextBlock x:Name="CenterSuit" Text="♠" FontSize="80" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                <!-- Wild Label -->
                <TextBlock x:Name="WildLabel" Text="WILD" Foreground="#FFD700" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,0" 
                           Visibility="Collapsed">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- Bottom Right Rank/Suit (Rotated) -->
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8" RenderTransformOrigin="0.5,0.5">
                    <StackPanel.RenderTransform>
                        <RotateTransform Angle="180"/>
                    </StackPanel.RenderTransform>
                    <TextBlock x:Name="BottomRank" Text="A" FontSize="32" FontWeight="Bold"/>
                    <TextBlock x:Name="BottomSuit" Text="♠" FontSize="32"/>
                </StackPanel>
                
                <!-- Held Overlay -->
                <Border x:Name="HeldOverlay" Background="#AA000000" CornerRadius="10" Visibility="Collapsed">
                    <Border BorderBrush="#00FFFF" BorderThickness="4" CornerRadius="10" Margin="4">
                        <TextBlock Text="HELD" Foreground="#00FFFF" FontWeight="Bold" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
                            <TextBlock.RenderTransform>
                                <RotateTransform Angle="-15"/>
                            </TextBlock.RenderTransform>
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="2"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Border>
                </Border>

                <!-- Win Overlay/Border -->
                <Border x:Name="WinBorder" BorderBrush="#FFFF00" BorderThickness="6" CornerRadius="12" Visibility="Collapsed">
                    <Border.Effect>
                        <DropShadowEffect Color="#FFFF00" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                    </Border.Effect>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
